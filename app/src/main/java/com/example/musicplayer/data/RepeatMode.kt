package com.example.musicplayer.data

//Simple Enum to pick the repeat mode
enum class RepeatMode {
    OFF,          // stop at end of queue
    REPEAT_ALL,   // loop the whole queue
    REPEAT_ONE    // loop the current song
}